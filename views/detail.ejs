<!DOCTYPE html>
<html>
  <head>
    <title>🌐<%= movie.title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
      <h1><%= movie.title %></h1>
        <nav> 
          <ul>
            <li><a href="/users/register">Registrarse</a></li>
            <li><a href="/users/login">Iniciar sesión</a></li>
            <li><a href="/users/logout">Cerrar sesión</a></li>
          </ul>
        </nav>
    </header>
    <ul>
      <li>Rating: <%= movie.rating %></li>
      <li>Awards: <%= movie.awards %></li>
      <li>Length: <%= movie.length %></li>
      <li>Release Date: <%= movie.release_date %></li>
      <li>Genere: <%=movie.genero.name%> </li>
      <li>Actores: <%=movie.actores.first_name%> <%=movie.actores.last_name%></li>
    </ul>
    <a href="/movies/edit/<%=movie.id%>"> <button>Editar pelicula</button></a>
    <form action="/movies/delete/<%=movie.id%>" method="POST">
        <button type="submit">Borrar pelicula</button>
    </form>
    <h3><a href="/movies">Volver al inicio</a></h3>
  </body>
</html>